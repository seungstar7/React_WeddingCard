// import fs from "node:module/fs";

export const remote = ()=>{
    let test = ["123","123"];
    // fs.readdir("./", (err, files) => {
    //     if (err) throw err;
    //     files.forEach((item) => {
    //         test.push(item);
    //         console.log(item);
    //     });
    //     console.log("readdir");
    // });
    return test;
}
